fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
sqlalchemy==2.0.36
aiosqlite==0.20.0
httpx==0.28.1
pydantic==2.10.4
pyyaml>=6.0
python-dotenv>=1.0.0

# Authentication (v5.0)
bcrypt>=4.0.0
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# Document parsing (lightweight — no Docling for deployment)
PyPDF2==3.0.1
python-docx==1.1.2

# OCR support (v5.2 — for image-only PDFs)
pytesseract>=0.3.10
pdf2image>=1.16.0

# v7.5.0 — image OCR (Pillow needed for png/jpg) + format handlers
Pillow>=10.0.0
openpyxl>=3.1.0
python-pptx>=0.6.23
beautifulsoup4>=4.12.0
striprtf>=0.0.26

# v9.0.0 — HEIC/HEIF support (iPhone photos default format)
pillow-heif>=0.18.0

# v9.0.0 Phase B v2 — Gemini multimodal (audio/video)
# Optional: ทำงานได้ต่อเมื่อ GOOGLE_API_KEY env var ถูก set
google-genai>=0.3.0

# v10.0.0 — LlamaParse for PDF (optional; off by default via USE_LLAMAPARSE_FOR_PDF)
# Falls back to Docling/PyPDF2/OCR if not installed or no API key
llama-parse>=0.5.0

# Stripe Payment removed in v9.6.0

# Google Drive BYOS (v7.0.0)
google-auth>=2.30.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.140.0
cryptography>=42.0.0

# Email Delivery (v7.6.0)
resend>=2.0.0

# LINE Bot Integration (v8.0.0)
line-bot-sdk>=3.11.0

# v11.0.0 — Hybrid clustering pipeline (BERTopic + RAPTOR + GraphRAG patterns)
# Plan: .agent-memory/plans/organize-refactor-v11.md
#
# ห้ามเปิดใช้งานก่อน USE_HYBRID_CLUSTERING=true (default OFF in config.py)
# Image size impact: +~80MB. Build time: +30s. Memory idle: +10MB.
#
# Why each:
# - numpy: vector ops (already pulled by other deps but pinning explicit)
# - scikit-learn: KMeans fallback + utilities for centrality calc
# - hdbscan: density-based clustering (auto-detect k, handles outliers)
# - umap-learn: dimensionality reduction (1536-d → 30-d before clustering)
# - networkx: graph data structure for entity/relationship graph
# - python-louvain: Leiden/Louvain community detection (replaces O(N²) LLM)
numpy>=1.26.0
scikit-learn>=1.4.0
hdbscan>=0.8.33
umap-learn>=0.5.5
networkx>=3.2.1
python-louvain>=0.16
