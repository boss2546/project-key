# Plan: v9.3.5.5 — Comprehensive Delete + Sync Cleanup (Full Loop)

**Author:** แดง (Daeng) · revised v3 (3 audit rounds · 10 findings)
**Date:** 2026-05-10
**Status:** `draft` — รอ user approve
**Foundation:** master HEAD `a165261` (v9.3.5.4 · live · verified)
**Target version:** **9.3.5.5** (or v9.4.0 ถ้า user prefer minor bump)
**Effort:** เขียว ~2.5-3 ชม. + ฟ้า ~45 นาที = **~3.5 ชม. total**
**Risk:** 🟡 MEDIUM (10 fixes · pattern reuse + 1 critical state-mgmt fix)

---

## 🎯 Goal

ปิด loophole ทั้งหมดของ delete + Drive cleanup + sync flow · ครอบคลุม edge cases ที่ค้นพบใน 3 รอบ audit หลัง v9.3.5.4 ship

**ผู้ใช้จะได้รับ (Best UX):**
1. ลบไฟล์ใน UI/MCP/Reset → หายทุกที่ครบ (disk + Drive raw + extracted + summary + vector)
2. กดปุ่ม Delete แล้ว response กลับเร็ว (~200ms) ไม่ค้าง 60-180s
3. Disconnect with keep_files=False แล้ว reconnect → **ไม่เกิดข้อมูลซ้ำใน Drive** (data integrity)
4. drive_picked files (user's external) → ไม่โดน trash โดยมิได้ตั้งใจ
5. ลบไฟล์ใน Drive UI โดยตรง → PDB UI ไม่แสดงไฟล์ผีอีก
6. ลบสำเร็จ/fail → user รู้ผลผ่าน UI feedback

**Developer ได้รับ (Simple Design):**
- Pattern reuse — ไม่สร้าง abstraction ใหม่
- 2 helpers + 4 endpoint patches + 1 sync logic refinement + 1 UI filter
- ไม่มี schema migration · ไม่มี dependency ใหม่

---

## 📚 Context — 10 Findings จาก 3 Audit Rounds

### Round 1 — เจอจาก user report "ลบในระบบ ไม่ลบใน Drive"
**F1.** MCP `_tool_delete_file` ขาด Drive + vector_search cleanup ([mcp_tools.py:1146](../../backend/mcp_tools.py#L1146))
**F2.** `/api/reset` (bulk delete) ขาด Drive cleanup → ไฟล์ "งอก" หลัง sync ([main.py:2739](../../backend/main.py#L2739))
**F3.** Sync `_pull_drive_to_local` Case A "stale link" → IntegrityError on keep_files=False reconnect
**F4.** Drive sub-folder orphans · `extracted/{file_id}.txt` + `summaries/{file_id}.md` ไม่ถูกลบ

### Round 2 — เจอจาก deep think "อะไรเล็กน้อยที่อาจ miss"
**F5.** `drive_picked` files (user's own files imported via Picker) — DELETE ปัจจุบันจะ trash ไฟล์ส่วนตัวของ user! 🚨
**F6.** DELETE endpoint timeout risk: 3 Drive calls × 60s = 180s · เกิน Cloudflare/Fly limit (~100s) → 504
**F7.** Sync orphan-cleanup ไม่มี retry budget → spam Drive API ทุก visibility-poll

### Round 3 — เจอจาก final thinking "มีอีกไหม"
**F16.** `deleted_in_drive` status ไม่ filter ใน `/api/files` → ไฟล์ผี · user งง
**F23.** DELETE return `{"status": "ok"}` ไม่บอกผล Drive cleanup → user ไม่รู้ว่า Drive ลบจริงไหม
**F24.** **keep_files=False reconnect = data duplication on Drive** 🚨🚨 (most critical)

### F24 Detailed — Critical state-mgmt bug

**Root cause:** Disconnect with keep_files=False ([main.py:3776](../../backend/main.py#L3776)):
```python
.values(drive_file_id=None, storage_source="local")
```

**Problem:** หลัง disconnect · cache rows ยังมี filename + content_hash · drive_file_id=NULL ·
storage_source='local'. Sync `_push_local_to_drive` filter:
```python
File.storage_source.in_([STORAGE_SOURCE_DRIVE_UPLOADED, "local"])
File.drive_file_id.is_(None)
```
→ matches all 5 rows → re-upload ทั้งหมด · Drive ตอนนี้มี 5 ไฟล์ใหม่ + 5 ไฟล์เก่า = **10 ไฟล์ซ้ำ**

Pull phase: เห็น Drive 10 ไฟล์ · 5 ใหม่ใน cache_files (after push set drive_file_id) · 5 เก่า → orphan-cleanup or stale-link races with push timestamps · cache state inconsistent

**Severity: 🔴 CRITICAL** — silent data duplication · user's Drive quota burned · cache desync

---

## 📁 Files to Create / Modify

### Backend (5 modify · 0 create)

- [ ] **`backend/storage_router.py`** (modify):
  - **NEW helpers (Step 1):**
    - `delete_extracted_text_from_drive_if_byos(user_id, db, file_id)` — find by name + delete (graceful invalid_grant pattern)
    - `delete_summary_from_drive_if_byos(user_id, db, file_id)` — find by name + delete
  - **NEW utility (Step 1.5):**
    - `_should_trash_drive_file(file_storage_source) -> bool` — guard for drive_picked (return False) · drive_uploaded (return True) · others (return False · safe default)
  - **NEW retry budget (Step 5b):**
    - `delete_drive_file_if_byos` รับ `attempt: int = 0` param สำหรับ logging (cap caller-side ใน sync)

- [ ] **`backend/main.py`** (modify):
  - **DELETE /api/files/{id}** (line 1696):
    - Background task pattern (Step 6a) — DB cleanup synchronous · Drive cleanup async via `BackgroundTasks`
    - Drive cleanup helper inline หรือแยก (`_cleanup_drive_for_deleted_file`)
    - Return cleanup stats (Step 7)
  - **DELETE /api/reset** (line 2739):
    - Per-file 4-6 step cleanup loop (Step 3 from original plan)
    - `storage_source` guard ใน Drive trash calls (Step 1.5)
    - Stats response (Step 7)
  - **GET /api/files** (line 1026 area):
    - Add filter: `WHERE processing_status != 'deleted_in_drive'` (Step 8 — F16)

- [ ] **`backend/mcp_tools.py`** (modify):
  - **`_tool_delete_file`** (line 1146):
    - Apply 6-step cleanup (Step 4)
    - Same `storage_source` guard
    - Background task NOT applicable (MCP synchronous) · accept slower response

- [ ] **`backend/drive_sync.py`** (modify):
  - `_pull_drive_to_local` Case A 3-way split (Step 5a):
    - Orphan: skip + retry-budget cleanup (Step 5b)
    - Stale-link: UPDATE existing row's drive_file_id (Step 5a)
    - Genuine-new: import via `_import_drive_file`
  - `_push_local_to_drive` guard (Step 5c — F24 critical fix):
    - **Pre-fetch Drive file list** (cached from pull's listing OR re-fetch)
    - Skip push ถ้า Drive มี file ที่ filename starts with `{file_id}_` (= we already pushed before · don't double-push)
  - SyncStats: `relinked: int` + `orphan_cleanup_skipped: int`
  - DriveSync class: in-memory `_orphan_retry_count: dict[str, int]` for retry budget

- [ ] **`backend/config.py`** (already at 9.4.0 — leave or revert per user choice; plan assumes 9.3.5.5 for clarity)

### Frontend (1 modify + 5 cache-bust)

- [ ] **`legacy-frontend/app.js`** (modify) — Step 7 (F23):
  - In `loadFiles()` หรือ delete handler · check response.drive_cleanup
  - Show subtle toast warning ถ้า drive_cleanup.raw === 'failed' หรือ 'pending'
  - Pattern: copy from existing upload-result toast logic
- [ ] 5 HTML cache-bust ?v=9.3.5.4 → ?v=9.3.5.5

### Memory (3 modify)
- [ ] `pipeline-state.md` — track build state
- [ ] `contracts/api-spec.md` — DELETE/reset/sync new contract details
- [ ] `project/decisions.md` — append STORAGE-008 (comprehensive delete contract)

---

## 📡 API Changes

### `DELETE /api/files/{file_id}` — Behavioral overhaul

**Pre v9.3.5.5:**
- Synchronous: disk + Drive raw (60s timeout risk) + vector + DB
- Response: `{"status": "ok"}`
- ห้ามแตะ drive_picked: ❌ (trashes anyway)

**Post v9.3.5.5:**
```python
@app.delete("/api/files/{file_id}")
async def delete_file(file_id, ..., background_tasks: BackgroundTasks):
    # Synchronous (fast):
    # 1. Disk: os.remove(raw_path)
    # 2. vector_search: remove_file
    # 3. DB: cascade delete (FileSummary, FileInsight, FileClusterMap)
    await db.delete(file)
    await db.commit()

    # Asynchronous (background · don't block response):
    if file.drive_file_id and _should_trash_drive_file(file.storage_source):
        background_tasks.add_task(
            _cleanup_drive_for_deleted_file,
            user_id=current_user.id,
            file_id=file.id,
            drive_file_id=file.drive_file_id,
            storage_source=file.storage_source,
        )

    return {
        "status": "ok",
        "drive_cleanup": _classify_cleanup(file)
        # "scheduled" if BYOS + drive_uploaded
        # "skipped:managed" if managed mode
        # "skipped:drive_picked" if drive_picked (user's external)
        # "skipped:no_drive_id" if drive_file_id NULL
    }
```

**Background task:**
```python
async def _cleanup_drive_for_deleted_file(user_id, file_id, drive_file_id, storage_source):
    """Async Drive cleanup (~3-180s timeout · don't block API response)."""
    async with AsyncSessionLocal() as db:
        try:
            # 1. Drive raw/
            await delete_drive_file_if_byos(user_id, db, drive_file_id)
            # 2. Drive extracted/
            await delete_extracted_text_from_drive_if_byos(user_id, db, file_id)
            # 3. Drive summaries/
            await delete_summary_from_drive_if_byos(user_id, db, file_id)
        except Exception as e:
            logger.warning(f"_cleanup_drive_for_deleted_file: {e}")
```

### `DELETE /api/reset` — Comprehensive cleanup

**Response shape (v9.3.5.5):**
```json
{
  "status": "ok",
  "message": "All data cleared",
  "stats": {
    "files_deleted": 60,
    "drive_files_trashed": 55,        // 5 อาจ skipped (drive_picked) หรือ failed
    "drive_extracted_trashed": 50,    // เฉพาะ files ที่มี extracted/
    "drive_summaries_trashed": 12,    // เฉพาะ organized
    "drive_cleanup_skipped_picked": 5, // F5 guard hits
    "vector_index_cleaned": 60,
    "errors": 3
  }
}
```

**Note:** `/api/reset` ใช้ synchronous loop (ไม่ใช้ background_tasks) เพราะ user คาดหวังว่า "Reset เสร็จ = ทุกอย่างเคลียร์" · slowness acceptable for explicit destructive action

### MCP `delete_file` tool — Behavioral overhaul

```json
{
  "status": "deleted",
  "filename": "report.pdf",
  "file_id": "abc123",
  "drive_cleanup": "scheduled" | "skipped:managed" | "skipped:drive_picked" | "skipped:no_drive_id"
}
```

MCP synchronous · no background tasks · slower (Drive cleanup blocks tool response · 1-180s)

### `POST /api/drive/sync` — Stats extension

```json
{
  "status": "ok",
  "stats": {
    "pulled_new": 0,                  // genuine new (drive_picked or external)
    "pulled_updated": 0,
    "pulled_deleted": 0,
    "pushed_new": 0,
    "pushed_updated": 0,
    "relinked": 5,                    // F3 — stale-link UPDATE existing row
    "orphans_cleaned": 3,             // F2/F4 — orphan trashed via sync
    "orphans_skipped_budget": 0,      // F7 — retry budget exhausted
    "duplicate_push_prevented": 0,    // F24 — push guard hits
    "conflicts_resolved": 0,
    "errors": 0,
    "duration_ms": 1234
  }
}
```

### `GET /api/files` — Filter `deleted_in_drive` (F16)

**Pre:** Returns ALL files including `processing_status='deleted_in_drive'`
**Post:** Default filter excludes them
- Optional query param `?include_deleted_in_drive=true` for admin/debug
- UI กดปุ่ม "Cleanup ghost files" ที่ Profile modal → DELETE /api/files/{id} for each ghost

---

## 💾 Data Model Changes

**ไม่มี** — schema เดิมพอ

---

## 🔧 Step-by-Step Implementation (สำหรับเขียว)

### Step 0 — Pre-flight check

```bash
# Verify dependencies
python -c "from backend.storage_router import _is_refresh_failure, _mark_drive_connection_errored, find_file_by_name; print('OK')"
python -c "from backend.drive_storage import DriveClient; print('OK')"

# Baseline regression
python scripts/byos_router_smoke.py  # 16/16 expected
python scripts/byos_foundation_smoke.py  # 26/26 expected
```

### Step 1 — Add 2 sub-folder delete helpers + storage guard utility

**File:** `backend/storage_router.py`

**1.1 — Add utility (top of file, near `_is_refresh_failure`):**
```python
# v9.3.5.5 — Drive trash guard for storage_source
# Why: drive_picked = user's external file imported via Picker · ห้าม trash · users data loss
# drive_uploaded = we created · safe to trash
# local / NULL = no Drive copy · skip
def _should_trash_drive_file(storage_source: str | None) -> bool:
    """Return True ถ้าควร trash file ใน Drive · ปกป้อง drive_picked (user's external)."""
    return storage_source == "drive_uploaded"
```

**1.2 — Add helper for extracted/ deletion (after `delete_drive_file_if_byos`):**
```python
async def delete_extracted_text_from_drive_if_byos(
    user_id: str,
    db: AsyncSession,
    file_id: str,
) -> bool:
    """Best-effort: trash extracted/{file_id}.txt · graceful invalid_grant.

    No-op for: managed users / not configured / not connected.
    Returns: True ถ้า trashed · False ถ้า skipped/failed
    """
    pair = await _get_byos_user_with_connection(user_id, db)
    if not pair:
        return False
    _user, conn = pair

    try:
        client = await _build_drive_client(conn)
        extracted_id = client.ensure_folder("extracted", parent_id=conn.drive_root_folder_id)
        filename = extracted_path_for(file_id).split("/")[-1]
        existing = client.find_file_by_name(filename, parent_id=extracted_id)
        if existing:
            client.delete_file(existing["id"])
            logger.info("BYOS: trashed extracted/%s for user %s", filename, user_id)
            return True
        return False  # already gone · graceful
    except Exception as e:
        if _is_refresh_failure(e):
            await _mark_drive_connection_errored(db, conn, e)
        logger.warning("BYOS: delete extracted/%s failed for user %s: %s", file_id, user_id, e)
        return False
```

**1.3 — Add helper for summaries/ deletion (mirror pattern):**
```python
async def delete_summary_from_drive_if_byos(
    user_id: str,
    db: AsyncSession,
    file_id: str,
) -> bool:
    """Best-effort: trash summaries/{file_id}.md · graceful invalid_grant."""
    pair = await _get_byos_user_with_connection(user_id, db)
    if not pair:
        return False
    _user, conn = pair

    try:
        client = await _build_drive_client(conn)
        summaries_id = await _get_summaries_folder_id(client, conn.drive_root_folder_id)
        filename = summary_path_for(file_id).split("/")[-1]
        existing = client.find_file_by_name(filename, parent_id=summaries_id)
        if existing:
            client.delete_file(existing["id"])
            logger.info("BYOS: trashed summaries/%s for user %s", filename, user_id)
            return True
        return False
    except Exception as e:
        if _is_refresh_failure(e):
            await _mark_drive_connection_errored(db, conn, e)
        logger.warning("BYOS: delete summaries/%s failed for user %s: %s", file_id, user_id, e)
        return False
```

**1.4 — Verify imports at top of `storage_router.py`:**
```python
from .drive_layout import (
    ...,
    extracted_path_for,  # ← already imported
    summary_path_for,    # ← already imported
)
```

**Self-test Step 1:**
```python
python -c "
from backend.storage_router import (
    delete_extracted_text_from_drive_if_byos,
    delete_summary_from_drive_if_byos,
    _should_trash_drive_file,
)
assert _should_trash_drive_file('drive_uploaded') == True
assert _should_trash_drive_file('drive_picked') == False
assert _should_trash_drive_file('local') == False
assert _should_trash_drive_file(None) == False
print('Step 1 OK · 2 helpers + guard imported · guard logic correct')
"
```

---

### Step 2 — Refactor DELETE /api/files/{id} with BackgroundTasks (F6 timeout fix)

**File:** `backend/main.py` (line 1696)

**2.1 — Add helper function (near _push_uploads_to_drive ~line 670):**
```python
async def _cleanup_drive_for_deleted_file(
    user_id: str,
    file_id: str,
    drive_file_id: str,
    storage_source: str,
) -> None:
    """Background task: trash Drive raw/extracted/summary for a deleted File row.

    Why: Drive API may timeout 60s/call · 3 calls × 60s = 180s · exceeds HTTP timeout
    Solution: run after API response · log success/fail · ไม่ block user

    Storage source guard: drive_picked → don't trash (user's external file)
    """
    from .database import AsyncSessionLocal
    from .storage_router import (
        _should_trash_drive_file,
        delete_drive_file_if_byos,
        delete_extracted_text_from_drive_if_byos,
        delete_summary_from_drive_if_byos,
    )

    if not _should_trash_drive_file(storage_source):
        logger.info(
            "delete_file cleanup: skipped %s · storage_source=%s",
            file_id, storage_source,
        )
        return

    async with AsyncSessionLocal() as bg_db:
        # Drive raw/
        try:
            await delete_drive_file_if_byos(user_id, bg_db, drive_file_id)
        except Exception as e:
            logger.warning(f"_cleanup_drive raw failed for {file_id}: {e}")
        # Drive extracted/
        try:
            await delete_extracted_text_from_drive_if_byos(user_id, bg_db, file_id)
        except Exception as e:
            logger.warning(f"_cleanup_drive extracted failed for {file_id}: {e}")
        # Drive summaries/
        try:
            await delete_summary_from_drive_if_byos(user_id, bg_db, file_id)
        except Exception as e:
            logger.warning(f"_cleanup_drive summary failed for {file_id}: {e}")
```

**2.2 — Replace existing DELETE handler:**
```python
@app.delete("/api/files/{file_id}")
async def delete_file(
    file_id: str,
    background_tasks: BackgroundTasks,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    """Delete file — fast synchronous DB/disk cleanup + async Drive cleanup (v9.3.5.5).

    Why background task: Drive trash can take up to 60s × 3 (raw + extracted + summary)
    = 180s total · exceeds Cloudflare/Fly HTTP timeout (~100s) · would 504.
    Now: API returns ~200ms · Drive cleanup runs in background · user sees fast response.

    drive_cleanup field tells client what to expect:
    - "scheduled" — Drive cleanup running in background (BYOS + drive_uploaded)
    - "skipped:managed" — managed mode, no Drive copy
    - "skipped:drive_picked" — user's external file, don't trash (F5 protection)
    - "skipped:no_drive_id" — file never made it to Drive
    """
    result = await db.execute(
        select(File).where(File.id == file_id, File.user_id == current_user.id)
    )
    file = result.scalar_one_or_none()
    if not file:
        raise HTTPException(status_code=404, detail="File not found")

    # Capture for background task before db.delete
    file_storage_source = file.storage_source
    file_drive_file_id = file.drive_file_id
    file_id_for_bg = file.id

    # 1. Disk
    if file.raw_path and os.path.exists(file.raw_path):
        try:
            os.remove(file.raw_path)
        except OSError as e:
            logger.warning(f"delete_file: remove raw_path {file.raw_path} failed: {e}")

    # 2. Vector search (sync · fast)
    try:
        from . import vector_search as _vs
        _vs.remove_file(file.id, user_id=current_user.id)
    except Exception as e:
        logger.warning(f"delete_file: vector_search remove failed for {file_id}: {e}")

    # 3. DB cascade (sync · fast)
    await db.delete(file)
    await db.commit()

    # 4. Drive cleanup (async background · don't block response)
    from .storage_router import _should_trash_drive_file
    drive_cleanup = "skipped:no_drive_id"
    if file_drive_file_id:
        if _should_trash_drive_file(file_storage_source):
            background_tasks.add_task(
                _cleanup_drive_for_deleted_file,
                current_user.id,
                file_id_for_bg,
                file_drive_file_id,
                file_storage_source,
            )
            drive_cleanup = "scheduled"
        elif file_storage_source == "drive_picked":
            drive_cleanup = "skipped:drive_picked"
            logger.info(
                f"delete_file: drive_picked file {file_id} unlinked · external preserved"
            )
        else:  # 'local' or other
            drive_cleanup = "skipped:managed"

    return {"status": "ok", "drive_cleanup": drive_cleanup}
```

**Self-test Step 2:**
```python
# Manual: delete a test file · check response includes drive_cleanup field
# Expected response time < 500ms (no Drive blocking)
```

---

### Step 3 — Refactor /api/reset (synchronous loop · stats response)

**File:** `backend/main.py` (line 2739)

**3.1 — Replace `reset_all`:**
```python
@app.delete("/api/reset")
async def reset_all(
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    """Comprehensive reset — synchronous cleanup with progress stats (v9.3.5.5).

    Why synchronous (vs background_tasks):
    User clicks "Reset all" expects "All cleared" feedback · stats accurate
    Slow but acceptable for explicit destructive action (~ 60 files × 3 calls × 5-60s).

    storage_source guard preserves drive_picked files (user's external).
    """
    from sqlalchemy import delete as sql_delete
    from .storage_router import (
        _should_trash_drive_file,
        delete_drive_file_if_byos,
        delete_extracted_text_from_drive_if_byos,
        delete_summary_from_drive_if_byos,
    )

    stats = {
        "files_deleted": 0,
        "drive_files_trashed": 0,
        "drive_extracted_trashed": 0,
        "drive_summaries_trashed": 0,
        "drive_cleanup_skipped_picked": 0,  # F5 guard
        "vector_index_cleaned": 0,
        "errors": 0,
    }

    # Clear graph data first (FK dependencies)
    await db.execute(sql_delete(SuggestedRelation).where(SuggestedRelation.user_id == current_user.id))
    await db.execute(sql_delete(GraphEdge).where(GraphEdge.user_id == current_user.id))
    await db.execute(sql_delete(GraphNode).where(GraphNode.user_id == current_user.id))
    await db.execute(sql_delete(NoteObject).where(NoteObject.user_id == current_user.id))
    await db.execute(sql_delete(GraphLens).where(GraphLens.user_id == current_user.id))

    # Files: full per-file cleanup
    files_result = await db.execute(select(File).where(File.user_id == current_user.id))
    for f in files_result.scalars().all():
        # 1. Disk
        if f.raw_path and os.path.exists(f.raw_path):
            try:
                os.remove(f.raw_path)
            except OSError as e:
                logger.warning(f"reset_all: remove raw_path {f.raw_path} failed: {e}")

        # 2. Drive raw/ (with storage_source guard)
        if f.drive_file_id and _should_trash_drive_file(f.storage_source):
            try:
                ok = await delete_drive_file_if_byos(current_user.id, db, f.drive_file_id)
                if ok:
                    stats["drive_files_trashed"] += 1
            except Exception as e:
                logger.warning(f"reset_all: drive raw delete failed for {f.id}: {e}")
                stats["errors"] += 1
        elif f.storage_source == "drive_picked":
            stats["drive_cleanup_skipped_picked"] += 1

        # 2.5. Drive extracted/ (only if drive_uploaded · we created it)
        if f.drive_file_id and _should_trash_drive_file(f.storage_source):
            try:
                ok = await delete_extracted_text_from_drive_if_byos(current_user.id, db, f.id)
                if ok:
                    stats["drive_extracted_trashed"] += 1
            except Exception as e:
                logger.warning(f"reset_all: drive extracted delete failed for {f.id}: {e}")
                stats["errors"] += 1

        # 2.7. Drive summaries/
        if f.drive_file_id and _should_trash_drive_file(f.storage_source):
            try:
                ok = await delete_summary_from_drive_if_byos(current_user.id, db, f.id)
                if ok:
                    stats["drive_summaries_trashed"] += 1
            except Exception as e:
                logger.warning(f"reset_all: drive summary delete failed for {f.id}: {e}")
                stats["errors"] += 1

        # 3. Vector search
        try:
            from . import vector_search as _vs
            _vs.remove_file(f.id, user_id=current_user.id)
            stats["vector_index_cleaned"] += 1
        except Exception as e:
            logger.warning(f"reset_all: vector remove failed for {f.id}: {e}")

        # 4. DB cascade
        await db.delete(f)
        stats["files_deleted"] += 1

    # Clusters
    clusters_result = await db.execute(select(Cluster).where(Cluster.user_id == current_user.id))
    for c in clusters_result.scalars().all():
        await db.delete(c)

    # Context packs
    packs_result = await db.execute(select(ContextPack).where(ContextPack.user_id == current_user.id))
    for p in packs_result.scalars().all():
        if p.md_path and os.path.exists(p.md_path):
            try:
                os.remove(p.md_path)
            except OSError as e:
                logger.warning(f"reset_all: remove pack md failed: {e}")
        await db.delete(p)

    await db.commit()

    logger.info(
        "reset_all: user=%s files=%d drive_raw=%d drive_extracted=%d drive_summaries=%d picked_skip=%d errors=%d",
        current_user.id[:8] + "..", stats["files_deleted"], stats["drive_files_trashed"],
        stats["drive_extracted_trashed"], stats["drive_summaries_trashed"],
        stats["drive_cleanup_skipped_picked"], stats["errors"],
    )

    return {"status": "ok", "message": "All data cleared", "stats": stats}
```

**Self-test Step 3:** Manual: setup test user · upload 3 files · /api/reset · check stats accurate

---

### Step 4 — Apply same pattern to MCP `_tool_delete_file`

**File:** `backend/mcp_tools.py` (line 1146)

**4.1 — Replace function:**
```python
async def _tool_delete_file(db: AsyncSession, user_id: str, file_id: str) -> dict:
    """Delete file with comprehensive cleanup (v9.3.5.5).

    MCP synchronous · no background_tasks support · accept slower response (~3-180s).
    Apply same 4-6 step + storage_source guard as HTTP DELETE.
    """
    if not file_id:
        raise ValueError("file_id is required")

    import os
    result = await db.execute(
        select(File).where(File.id == file_id, File.user_id == user_id)
    )
    file = result.scalar_one_or_none()
    if not file:
        return {"error": "File not found"}

    filename = file.filename
    storage_source = file.storage_source
    drive_file_id = file.drive_file_id
    drive_cleanup = "skipped:no_drive_id"

    # 1. Disk
    if file.raw_path and os.path.exists(file.raw_path):
        try:
            os.remove(file.raw_path)
        except OSError as e:
            logger.warning(f"mcp delete_file: remove raw_path failed: {e}")

    # 2. Drive cleanup (synchronous in MCP · accept slower)
    if drive_file_id:
        from .storage_router import (
            _should_trash_drive_file,
            delete_drive_file_if_byos,
            delete_extracted_text_from_drive_if_byos,
            delete_summary_from_drive_if_byos,
        )
        if _should_trash_drive_file(storage_source):
            try:
                await delete_drive_file_if_byos(user_id, db, drive_file_id)
                await delete_extracted_text_from_drive_if_byos(user_id, db, file_id)
                await delete_summary_from_drive_if_byos(user_id, db, file_id)
                drive_cleanup = "completed"
            except Exception as e:
                logger.warning(f"mcp delete_file: drive cleanup failed: {e}")
                drive_cleanup = "failed"
        elif storage_source == "drive_picked":
            drive_cleanup = "skipped:drive_picked"
        else:
            drive_cleanup = "skipped:managed"

    # 3. Vector search
    try:
        from . import vector_search as _vs
        _vs.remove_file(file.id, user_id=user_id)
    except Exception as e:
        logger.warning(f"mcp delete_file: vector remove failed: {e}")

    # 4. DB cascade
    await db.delete(file)
    await db.commit()

    return {
        "status": "deleted",
        "filename": filename,
        "file_id": file_id,
        "drive_cleanup": drive_cleanup,
    }
```

---

### Step 5 — Sync `drive_sync.py` enhancements (F3 + F7 + F24)

**File:** `backend/drive_sync.py`

**5.1 — Update `SyncStats` TypedDict (top of file):**
```python
class SyncStats(TypedDict):
    """ผลลัพธ์รวมของ sync รอบนึง — สำหรับ logging + UI status."""
    pulled_new: int
    pulled_updated: int
    pulled_deleted: int
    pushed_new: int
    pushed_updated: int
    relinked: int                      # v9.3.5.5 — F3 stale-link UPDATE
    orphans_cleaned: int               # v9.3.5.5 — F2/F4 sync cleanup hits
    orphans_skipped_budget: int        # v9.3.5.5 — F7 retry exhausted
    duplicate_push_prevented: int      # v9.3.5.5 — F24 push guard
    conflicts_resolved: int
    errors: int
    duration_ms: int
```

**5.2 — Update `DriveSync.__init__` for retry budget:**
```python
def __init__(self, user_id: str, db: AsyncSession):
    self.user_id = user_id
    self.db = db
    self._client: Optional[DriveClient] = None
    self._connection: Optional[DriveConnection] = None
    self._folder_layout: dict[str, str] = {}
    # v9.3.5.5 — F7 in-memory retry budget (per-session · resets on server restart)
    self._orphan_retry_count: dict[str, int] = {}
    self._orphan_retry_max: int = 3
```

**5.3 — Update `run_full_sync` stats init:**
```python
stats: SyncStats = {
    "pulled_new": 0, "pulled_updated": 0, "pulled_deleted": 0,
    "pushed_new": 0, "pushed_updated": 0,
    "relinked": 0, "orphans_cleaned": 0,
    "orphans_skipped_budget": 0, "duplicate_push_prevented": 0,
    "conflicts_resolved": 0, "errors": 0, "duration_ms": 0,
}
```

**5.4 — Refactor `_pull_drive_to_local` Case A (3-way + retry budget):**
```python
async def _pull_drive_to_local(self, stats: SyncStats) -> None:
    """List Drive's raw/ folder + reconcile cache (v9.3.5.5).

    Cases:
        A1. Drive file pattern match + local_id NOT in DB → ORPHAN (skip + retry-budgeted cleanup)
        A2. Drive file pattern match + local_id IN DB → STALE-LINK (UPDATE row's drive_file_id)
        A3. Drive file no pattern (drive_picked) → IMPORT (genuine new)
        B. Drive id IN cache_files → drift check (Drive wins)
        C. Cache has drive_file_id but Drive lacks → SOFT-DELETE cache
    """
    assert self._client is not None
    raw_folder_id = self._folder_layout["raw"]
    drive_files = self._client.list_folder(raw_folder_id, only_files=True)
    drive_ids = {f["id"] for f in drive_files}

    # Build cache index: drive_file_id -> File row
    result = await self.db.execute(
        select(File).where(
            File.user_id == self.user_id,
            File.drive_file_id.isnot(None),
        )
    )
    cache_files: dict[str, File] = {
        f.drive_file_id: f for f in result.scalars().all() if f.drive_file_id
    }

    # Build set of all File ids (for stale-link detection)
    result_all = await self.db.execute(
        select(File.id).where(File.user_id == self.user_id)
    )
    all_local_file_ids = {row[0] for row in result_all}

    for drive_f in drive_files:
        drive_id = drive_f["id"]
        cached = cache_files.get(drive_id)
        if not cached:
            # 3-way Case A split
            name_in_drive = drive_f.get("name", "")
            local_id, _ = self._split_drive_name(name_in_drive)
            is_app_upload_pattern = (
                len(name_in_drive) > 13
                and name_in_drive[12] == "_"
                and all(c in "0123456789abcdef" for c in local_id)
            )

            if is_app_upload_pattern:
                if local_id not in all_local_file_ids:
                    # A1 — ORPHAN with retry budget (F7)
                    attempts = self._orphan_retry_count.get(drive_id, 0)
                    if attempts >= self._orphan_retry_max:
                        stats["orphans_skipped_budget"] += 1
                        logger.warning(
                            "BYOS sync: orphan retry budget exhausted for %s (attempts=%d)",
                            drive_id, attempts,
                        )
                        continue
                    try:
                        self._client.delete_file(drive_id)
                        stats["orphans_cleaned"] += 1
                        # Success — clear retry counter
                        self._orphan_retry_count.pop(drive_id, None)
                        logger.info(
                            "BYOS sync: cleaned up orphan Drive file %s (id=%s)",
                            drive_id, local_id,
                        )
                    except Exception as e:
                        self._orphan_retry_count[drive_id] = attempts + 1
                        logger.warning(
                            "BYOS sync: orphan cleanup failed for %s (attempt %d/%d): %s",
                            drive_id, attempts + 1, self._orphan_retry_max, e,
                        )
                    continue

                # A2 — STALE-LINK (F3)
                stale_row_q = await self.db.execute(
                    select(File).where(
                        File.id == local_id,
                        File.user_id == self.user_id,
                    )
                )
                stale_row = stale_row_q.scalar_one_or_none()
                if stale_row:
                    stale_row.drive_file_id = drive_id
                    stale_row.drive_modified_time = _parse_drive_time(drive_f["modifiedTime"])
                    stale_row.storage_source = STORAGE_SOURCE_DRIVE_UPLOADED
                    stats["relinked"] += 1
                    logger.info(
                        "BYOS sync: re-linked id=%s to drive_id=%s",
                        local_id, drive_id,
                    )
                continue

            # A3 — GENUINE NEW (drive_picked or external)
            self._import_drive_file(drive_f, stats)
        else:
            # CASE B — drift?
            if _has_drift(cached.drive_modified_time, drive_f["modifiedTime"]):
                cached.drive_modified_time = _parse_drive_time(drive_f["modifiedTime"])
                stats["pulled_updated"] += 1
                stats["conflicts_resolved"] += 1

    # Case C unchanged
    for drive_id, cached in cache_files.items():
        if drive_id not in drive_ids:
            cached.processing_status = "deleted_in_drive"
            stats["pulled_deleted"] += 1
```

**5.5 — Add F24 fix in `_push_local_to_drive` (push guard):**
```python
async def _push_local_to_drive(self, stats: SyncStats) -> None:
    """Upload files needing Drive push (v9.3.5.5 — F24 duplicate guard).

    Why guard: keep_files=False disconnect → drive_file_id=NULL on cache rows
    → after reconnect, push would re-upload all files → Drive ends up with duplicates
    Pre-fetch Drive raw/ listing → skip files where Drive already has matching pattern.
    """
    assert self._client is not None
    raw_folder_id = self._folder_layout["raw"]

    # v9.3.5.5 — Pre-fetch Drive listing for duplicate detection (F24)
    try:
        drive_files = self._client.list_folder(raw_folder_id, only_files=True)
        drive_filename_set = {f["name"] for f in drive_files}
    except Exception as e:
        logger.warning("BYOS push: failed to pre-fetch Drive listing: %s", e)
        drive_filename_set = set()  # fall back to no-guard

    result = await self.db.execute(
        select(File).where(
            File.user_id == self.user_id,
            File.storage_source.in_([STORAGE_SOURCE_DRIVE_UPLOADED, "local"]),
            File.drive_file_id.is_(None),
        )
    )
    pending = list(result.scalars().all())
    if not pending:
        return

    raw_folder_id = self._folder_layout["raw"]
    for f in pending:
        # v9.3.5.5 — F24 guard: skip if Drive already has matching app-pattern file
        # Why: prevents re-upload after keep_files=False reconnect (data dup)
        expected_drive_name = f"{f.id}_{f.filename}"
        if expected_drive_name in drive_filename_set:
            # Find the matching Drive file ID + relink instead of re-upload
            matching = next(
                (df for df in drive_files if df["name"] == expected_drive_name),
                None,
            )
            if matching:
                f.drive_file_id = matching["id"]
                f.drive_modified_time = _parse_drive_time(matching["modifiedTime"])
                f.storage_source = STORAGE_SOURCE_DRIVE_UPLOADED
                stats["duplicate_push_prevented"] += 1
                logger.info(
                    "BYOS push: prevented duplicate · re-linked %s to existing Drive %s",
                    f.id, matching["id"],
                )
                continue
        # ... rest of push logic unchanged ...
        try:
            # existing upload code
            ...
        except Exception as e:
            ...
```

---

### Step 6 — `/api/files` filter `deleted_in_drive` (F16)

**File:** `backend/main.py` (around line 1026)

**6.1 — Add filter:**
```python
@app.get("/api/files")
async def list_files(
    include_deleted_in_drive: bool = Query(False),
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    """List user's files · default excludes deleted_in_drive (F16)."""
    query = select(File).where(File.user_id == current_user.id)
    if not include_deleted_in_drive:
        query = query.where(File.processing_status != "deleted_in_drive")
    query = query.order_by(File.uploaded_at.desc())
    result = await db.execute(query)
    files = result.scalars().all()
    # ... existing serialization ...
```

---

### Step 7 — Frontend cleanup feedback (F23)

**File:** `legacy-frontend/app.js` (find delete handler, ~line ?)

**7.1 — In delete success handler:**
```javascript
async function deleteFile(fileId) {
  const res = await authFetch(`/api/files/${fileId}`, { method: 'DELETE' });
  if (!res.ok) {
    showToast(getLang() === 'th' ? 'ลบไม่สำเร็จ' : 'Delete failed', 'error');
    return;
  }
  const data = await res.json();
  // v9.3.5.5 — show subtle warning if Drive cleanup failed/skipped (F23)
  const cleanup = data.drive_cleanup;
  if (cleanup === 'scheduled') {
    showToast(
      getLang() === 'th' ? 'ลบสำเร็จ · กำลังทำความสะอาด Drive' : 'Deleted · cleaning Drive',
      'success'
    );
  } else if (cleanup === 'skipped:drive_picked') {
    showToast(
      getLang() === 'th'
        ? 'ลบจากระบบแล้ว · ไฟล์ต้นฉบับใน Drive ของคุณยังอยู่ (เพราะคุณ import เข้ามาเอง)'
        : 'Removed from system · original Drive file preserved (you imported it)',
      'info'
    );
  } else {
    showToast(getLang() === 'th' ? 'ลบสำเร็จ' : 'Deleted', 'success');
  }
  loadFiles();
  loadStats();
}
```

---

### Step 8 — Bump version + cache-bust + memory

**8.1** `backend/config.py`:
```python
APP_VERSION = "9.3.5.5"
# (or keep "9.4.0" if user prefers minor bump · plan agnostic)
```

**8.2** Cache-bust 5 HTML files:
```bash
sed -i 's/?v=9\.3\.5\.4/?v=9.3.5.5/g' legacy-frontend/{app,landing,admin,auth-line,shared_pack}.html
sed -i 's/v9\.3\.5\.4</v9.3.5.5</g' legacy-frontend/app.html
sed -i 's/Admin · v9\.3\.5\.4</Admin · v9.3.5.5</g' legacy-frontend/admin.html
```

**8.3** Update memory (pipeline-state, api-spec, decisions STORAGE-008)

---

## 🧪 Test Scenarios (สำหรับฟ้า)

### A. Happy Path (regression)
- A1 Managed user: DELETE → drive_cleanup="skipped:managed" · response < 200ms
- A2 Managed user: /api/reset → stats all drive_* = 0
- A3 Managed user: MCP delete_file → drive_cleanup="skipped:managed"

### B. BYOS Drive_Uploaded delete
- B1 BYOS + drive_uploaded: DELETE → cleanup="scheduled" · response fast · background trashes 3 entries
- B2 Sync after delete → orphans_cleaned=0 (background already trashed) · pulled_new=0
- B3 ถ้า background fails: next sync orphans_cleaned should clean up

### C. BYOS Drive_Picked delete (F5 critical)
- C1 BYOS + drive_picked: DELETE → drive_cleanup="skipped:drive_picked"
- C2 Drive UI verify: original file STILL EXISTS · ห้ามถูก trash
- C3 sync after: pulled_new=1 (Drive imports back as drive_picked) · expected behavior

### D. BYOS DELETE timeout protection (F6)
- D1 Mock Drive API to hang 90s · DELETE → response < 500ms · background still running
- D2 Eventually timeout → log warning · DB clean · drive_file_id remains on Drive (orphan)
- D3 Next sync → orphan-cleanup tries again (with retry budget)

### E. /api/reset comprehensive
- E1 BYOS user 60 mixed (50 drive_uploaded · 10 drive_picked) → stats:
  - files_deleted: 60
  - drive_files_trashed: 50
  - drive_cleanup_skipped_picked: 10
  - drive_extracted_trashed: ~50 (only uploaded ones)
  - drive_summaries_trashed: ~20 (only organized)
- E2 Drive UI verify: 10 picked files preserved · 50 uploaded trashed
- E3 Sync after reset → orphans_cleaned=0 (Drive already clean)

### F. F24 — keep_files=False reconnect (CRITICAL)
- F1 Setup: BYOS + 5 drive_uploaded files · disconnect with keep_files=False
- F2 Verify: cache rows · drive_file_id=NULL · storage_source='local'
- F3 Reconnect → sync runs:
  - _push_local_to_drive: pre-fetch Drive listing · sees 5 files matching pattern
  - Push guard hits 5 times → stats.duplicate_push_prevented=5
  - drive_file_id RE-LINKED to existing Drive files
  - Drive count after: STILL 5 (no duplicates!)
- F4 Stats: pushed_new=0, duplicate_push_prevented=5, relinked=0 (handled by push guard)

### G. F3 stale-link genuine case
- G1 Setup: User reconnect to DIFFERENT Drive account (no existing files)
- G2 Sync: push 5 NEW files · pull sees them · no stale-link · normal flow
- G3 Old drive_file_ids in DB never matched · cleared by push guard? → tested in G

### H. F16 — deleted_in_drive UI filter
- H1 Setup: file with processing_status='deleted_in_drive' (manually update DB)
- H2 GET /api/files (default) → file NOT in response
- H3 GET /api/files?include_deleted_in_drive=true → file in response (admin/debug)

### I. F7 — Orphan retry budget
- I1 Mock 5 orphan files · Drive delete always fails
- I2 Sync run 1: orphans_cleaned=0, errors=5, retry_count[drive_id]=1
- I3 Sync run 2: retry_count=2 · still failing
- I4 Sync run 3: retry_count=3 · still failing
- I5 Sync run 4: orphans_skipped_budget=5 · no Drive API calls (saved)
- I6 Server restart → retry counter resets · tries again

### J. Concurrent operations
- J1 2 concurrent DELETE for same file_id → 1 success + 1 404 · DB consistent
- J2 DELETE while sync running → DB lock · sequential · no race
- J3 Upload + reset overlap → upload's row may survive reset · acceptable (rare)

### K. Regression suite
- K1 byos_router_smoke: 16 PASS + 4 new cases (helpers + guards) = 20+
- K2 byos_foundation_smoke: 26/26 PASS
- K3 byos_sync_smoke (if exists): + 6 new cases (3-way Case A · push guard · retry budget)
- K4 Manual: pre-existing flows (upload, organize, share) ไม่กระทบ

---

## ✅ Done Criteria

- [ ] **Code:**
  - [ ] 2 helpers + 1 utility ใน storage_router.py
  - [ ] DELETE /api/files: BackgroundTasks pattern · drive_cleanup field
  - [ ] /api/reset: per-file loop · stats response · storage_source guard
  - [ ] MCP _tool_delete_file: 6-step + storage_source guard
  - [ ] drive_sync: SyncStats extended · _pull 3-way Case A · _push F24 guard · retry budget
  - [ ] /api/files: filter deleted_in_drive (F16)
  - [ ] Frontend deleteFile: handle drive_cleanup field (F23)
  - [ ] APP_VERSION 9.3.5.4 → 9.3.5.5 (or 9.4.0)
  - [ ] Cache-bust 21 refs at new version
- [ ] **Behavior verified manually:**
  - [ ] BYOS drive_uploaded delete → response < 500ms · background cleans 3 Drive entries
  - [ ] BYOS drive_picked delete → original Drive file preserved
  - [ ] /api/reset 60 mixed files → stats accurate
  - [ ] keep_files=False reconnect → no Drive duplicates
  - [ ] sync orphan retry budget → max 3 attempts per orphan per session
- [ ] **Tests:** byos_router (20+) · byos_foundation (26) · byos_sync (extended) all PASS
- [ ] **Memory updated:** pipeline-state · api-spec (extended sections) · decisions STORAGE-008
- [ ] **Convention:** ทุก commit `Author-Agent: เขียว (Khiao)` · TH comments WHY · no debug

---

## ⚠️ Risks / Open Questions

### Risks (พร้อม mitigation)

**R1 — BackgroundTasks queue overflow ตอน /api/reset 1000 files**
- Impact: HIGH · /api/reset ใช้ synchronous loop (ไม่ใช่ background_tasks) → no queue · OK
- DELETE single file ใช้ background_tasks · 1 task per delete · FastAPI queue handles
- Mitigation: accept · monitor logs

**R2 — F24 push guard pre-fetch Drive listing fails**
- Fall-back: drive_filename_set = empty · push proceeds (potential duplicates)
- Worst case: same as before fix · not regression
- Mitigation: log warning · accept

**R3 — F24 stale-link race with push: same row updated twice**
- Pull's stale-link path AND push's pre-link could race
- Last-write-wins · drive_file_id same value · no data corruption
- Mitigation: accept rare race

**R4 — Orphan retry budget exhausted = file stuck on Drive forever**
- After 3 attempts · sync skips · file remains on Drive (storage waste)
- Recovery: server restart resets counter · OR user manually delete from Drive UI
- Mitigation: log warning · doc as known limit

**R5 — Background Drive cleanup ล้มเหลว · user ไม่รู้**
- Frontend gets "scheduled" but cleanup may fail silently
- Sync orphan-cleanup retries · eventually succeeds OR budget exhausted
- F23 toast tells user "scheduled" so they know it's async
- Mitigation: accept eventual consistency

### Open Questions

**Q1 — Should `/api/reset` use BackgroundTasks too?**
- Pro: Faster response (returns 200 in ~100ms)
- Con: User can't see stats immediately · UX worse for explicit destructive action
- Default: NO · synchronous · accept slowness
- Reconsider in v9.4.x with proper job queue

**Q2 — Should `delete_extracted_text_from_drive_if_byos` cache the folder ID?**
- Currently: ensure_folder("extracted") every call (~1 API call extra)
- Could cache per-DriveSync instance · saves calls
- Default: NO for v9.3.5.5 · simple · existing pattern

**Q3 — F18 pattern false-positive (12-char hex collision)?**
- 281 trillion combinations · negligible risk
- Could add stricter validation (filename matches cache row's filename) · complex
- Default: ACCEPT as known limitation

**Q4 — Should we bump to v9.4.0 (already in config.py)?**
- Scope ใหญ่ (10 fixes · cross-cutting) → 9.4.0 reasonable
- BUT existing v9.4.0 plan (upload-queue) needs renaming to v9.5.0
- Default: leave decision to user · plan works either name

**Q5 — Should F8/F9 (reprocess/promote update Drive extracted) be added?**
- Drive content drift after reprocess
- Rare flow · low impact
- Default: DEFER to v9.4.x

---

## 📌 Notes for เขียว

### Implementation order (recommended)
1. **Step 1** (helpers + guard) — foundation for all others
2. **Step 2** (DELETE endpoint) — uses Step 1
3. **Step 3** (/api/reset) — uses Step 1
4. **Step 4** (MCP delete) — uses Step 1
5. **Step 5** (sync · most complex) — independent · do last in backend phase
6. **Step 6** (/api/files filter) — small · fast
7. **Step 7** (frontend toast) — small · fast
8. **Step 8** (version + cache-bust + memory) — last

### Test after EACH step
**สำคัญ:** User สั่ง "อย่าลืมเทสต์ทุกครั้งที่เสร็จในแต่ละพัก"
- After Step 1: import test · helper signatures · _should_trash_drive_file unit test
- After Step 2: manual DELETE on test file · check response < 500ms
- After Step 3: /api/reset on small test data · check stats
- After Step 4: MCP delete via Claude Desktop or test script
- After Step 5: byos_sync_smoke + manual keep_files=False reconnect simulation
- After Step 6: GET /api/files · verify filter
- After Step 7: Manual UI delete · check toast text
- After Step 8: All regression suites · APP_VERSION verify

### Gotchas
1. **`extracted_path_for(file_id)`** returns `"extracted/{file_id}.txt"` · ต้อง `.split("/")[-1]` for filename
2. **`summary_path_for(file_id)`** same pattern
3. **`SyncStats` TypedDict** add 4 new fields ก่อน · Pylance จะ complain
4. **`DriveSync.__init__`** เพิ่ม `_orphan_retry_count` + `_orphan_retry_max` · existing tests ที่ inject mock client ผ่าน `_from_client` ต้อง init field เหล่านี้ด้วย
5. **`BackgroundTasks`** import: `from fastapi import BackgroundTasks` (ที่ main.py นั้นน่าจะมีแล้ว · ตรวจ)
6. **`AsyncSessionLocal`** ใน background task: `from .database import AsyncSessionLocal · async with AsyncSessionLocal() as bg_db: ...`
7. **`/api/files` filter** อาจจะ break existing frontend ที่ assume ทุกไฟล์ return · verify file list page ยัง work

### Effort breakdown
- Step 1: ~15 นาที (3 helpers/utils)
- Step 2: ~20 นาที (BackgroundTasks pattern · careful capture variables)
- Step 3: ~15 นาที (loop + stats)
- Step 4: ~10 นาที (mirror Step 2-3 logic)
- Step 5: ~30 นาที (sync logic complex · test carefully)
- Step 6: ~5 นาที (1-line filter)
- Step 7: ~10 นาที (frontend toast)
- Step 8: ~10 นาที (version + memory)
- Self-test inter-step: ~15 นาที total
- **Total: ~2-2.5 ชม.**

### Commit plan (8 commits — small + logical)
```
1. feat(storage_router): 2 sub-folder delete helpers + storage_source guard [v9.3.5.5]
2. fix(api): DELETE /api/files BackgroundTasks + drive_picked guard + stats [v9.3.5.5]
3. fix(api): /api/reset comprehensive cleanup loop with stats [v9.3.5.5]
4. fix(mcp): _tool_delete_file 6-step + storage_source guard [v9.3.5.5]
5. fix(sync): Case A 3-way + push duplicate guard + retry budget [v9.3.5.5]
6. fix(api): /api/files filter deleted_in_drive [v9.3.5.5]
7. feat(frontend): drive_cleanup feedback toast [v9.3.5.5]
8. chore: APP_VERSION 9.3.5.5 + cache-bust + memory STORAGE-008 [v9.3.5.5]
```

---

## 🔄 Pipeline Next Steps

```
แดง plan ✅ (this file · v3 comprehensive · 10 fixes)
    ↓
User review + approve scope (Tier A/B/C)
    ↓
แดง update pipeline-state → "plan_approved"
    ↓
เขียว build (~2-2.5 ชม. · test after each step per user instruction)
    ↓
ฟ้า review + run smoke + manual prod test (~45 นาที)
    ↓
APPROVE → user push + flyctl deploy
    ↓
✅ v9.3.5.5 LIVE — comprehensive delete/sync cleanup · all 10 edge cases closed
```

**Total pipeline:** ~3.5 ชม. (build 2.5 + review 0.75 + deploy 0.25)

---

**End of Plan v9.3.5.5 (revised v3 — comprehensive · 10 findings · full loop)**
